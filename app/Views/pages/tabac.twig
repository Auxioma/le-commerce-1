{% set heroEyebrow = 'TABAC' %}
{% set heroText = "Votre bureau de tabac de proximité : cigarettes, tabac à rouler, cigares, cigarette électronique et tous les accessoires du quotidien." %}
{% set heroActions = [
    {href: 'tel:' ~ shop.phone_href, label: 'Appeler le commerce', class: 'btn-primary'},
] %}
{% set heroSlug = 'hero_tabac' %}
{% include 'partials/page-hero.twig' %}

<section class="max-w-[1536px] mx-auto px-6 lg:px-10 py-12 space-y-6">

  {% set gridTitle = 'NOTRE OFFRE TABAC' %}
  {% set gridItems = categories %}
  {% set gridCols = 2 %}
  {% include 'partials/feature-grid.twig' %}

  <div class="grid lg:grid-cols-[1.3fr_0.7fr] gap-6">
    <div class="card card-md">
      <h2 class="font-bold text-ink mb-4">Autres services disponibles au bureau de tabac</h2>
      <ul class="space-y-2.5">
        {% for service in services %}
          <li class="flex items-center gap-3 text-sm text-gray-600">
            <span class="w-6 h-6 rounded-md bg-brand-50 text-brand-500 flex items-center justify-center shrink-0">
              <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
            </span>
            {{ service }}
          </li>
        {% endfor %}
      </ul>
    </div>

    <div class="card card-md bg-ink text-white">
      <p class="text-xs uppercase tracking-[0.25em] font-bold text-gray-400 mb-3">Vente réglementée</p>
      <p class="text-sm leading-relaxed text-gray-300">La vente de produits du tabac est interdite aux mineurs de moins de 18 ans. Une pièce d'identité pourra être demandée en caisse.</p>
    </div>
  </div>

  <div class="card card-md flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
    <div>
      <h2 class="font-bold text-ink mb-1">Horaires du bureau de tabac</h2>
      <p class="text-sm text-gray-500">Lundi au Samedi : {{ shop.hours.lun_sam }} · Dimanche : {{ shop.hours.dim }}</p>
    </div>
    <a href="{{ base_path }}/contact" class="btn-primary shrink-0">Nous contacter</a>
  </div>
</section>
